{
  "output": {
    "design": {
      "pattern": "hourly_rollup",
      "rationale": "campaign_day_agg uses hourly staging with daily rollup. Affiliate actions arrive frequently enough for hourly incremental. Single model since there's no existing aggregate to modify.",
      "layers": [
        {"name": "staging", "models": ["stg_acquisition__affiliate_actions"]}
      ]
    },
    "models": [
      {
        "name": "stg_acquisition__affiliate_actions",
        "layer": "staging",
        "materialization": "incremental",
        "sql_content": "{{ config(materialized='incremental', schema='acquisition', tags=['acquisition', 'hourly'], unique_key='md5_key') }}\n\n-- [SOURCE] Impact Radius affiliate actions from Fivetran\nWITH source AS (\n    SELECT * FROM {{ source('impact_radius', 'affiliate_actions') }}\n    {% if is_incremental() %}\n    WHERE _fivetran_synced > (SELECT MAX(_fivetran_synced) FROM {{ this }})\n    {% endif %}\n),\n\n-- [RENAME] Standardize to affiliate schema\nrenamed AS (\n    SELECT\n        action_id,\n        action_date,\n        campaign_id,\n        campaign_name,\n        COALESCE(sub_id, 'unknown') AS sub_id,\n        revenue AS revenue_usd,\n        payout AS payout_usd,\n        country_code AS country,\n        _fivetran_synced\n    FROM source\n),\n\n-- [FINAL]\nSELECT\n    {{ dbt_utils.generate_surrogate_key(['action_id']) }} AS md5_key,\n    *\nFROM renamed",
        "schema_yml_content": "models:\n  - name: stg_acquisition__affiliate_actions\n    description: Staging model for Impact Radius affiliate actions\n    columns:\n      - name: md5_key\n        description: Surrogate key (action_id)\n        tests:\n          - unique\n          - not_null\n      - name: action_id\n        tests:\n          - not_null\n      - name: action_date\n        tests:\n          - not_null",
        "sources_yml_content": "sources:\n  - name: impact_radius\n    database: FIVETRAN_DATABASE\n    schema: IMPACT_RADIUS\n    tables:\n      - name: affiliate_actions\n        loaded_at_field: _fivetran_synced\n        freshness:\n          warn_after: {count: 6, period: hour}\n          error_after: {count: 12, period: hour}",
        "file_path": "models/mart/acquisition/stage/stg_acquisition__affiliate_actions.sql",
        "schema_yml_path": "models/mart/acquisition/stage/schema.yml",
        "is_new": true,
        "is_modification": false
      }
    ],
    "dag_recommendation": {
      "existing_dag": "transform_acquisition__hourly",
      "tags": ["acquisition", "hourly"],
      "schedule": "hourly",
      "needs_new_dag": false
    }
  },
  "decision_log": {
    "decision": "hourly_rollup pattern — 1 new incremental staging model following campaign_day_agg template",
    "rationale": "campaign_day_agg uses hourly staging with daily rollup. Affiliate actions arrive frequently enough for hourly incremental.",
    "alternatives_considered": ["multi_source_union — rejected, no existing agg to extend", "standard — rejected, hourly source needs incremental"],
    "confidence": "high",
    "informed_by": ["reference_examples", "profiling", "domain_conventions"]
  },
  "tool_calls": [],
  "token_usage": {"prompt": 12000, "completion": 5000}
}
